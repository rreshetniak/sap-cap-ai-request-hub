_schema-version: 3.3.0
ID: sap-cap-ai-request-hub
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: sap-cap-ai-request-hub-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: sap-cap-ai-request-hub-auth
      - name: sap-cap-ai-request-hub-db
      - name: sap-cap-ai-request-hub-destination

  - name: sap-cap-ai-request-hub-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: sap-cap-ai-request-hub-db

resources:
  - name: sap-cap-ai-request-hub-auth
    type: org.cloudfoundry.existing-service
    parameters:
      service-name: request-hub-auth
  - name: sap-cap-ai-request-hub-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: sap-cap-ai-request-hub-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
