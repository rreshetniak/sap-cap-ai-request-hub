# Portfolio Screenshots

Add three clean screenshots from the local/BAS Fiori preview with the exact filenames below:

1. `request-list.png` — List Report with several mock requests, filters, status, priority, and assigned processor visible.
2. `request-details.png` — Object Page showing request header/details plus the business-history or approval section.
3. `reject-dialog.png` — custom SAPUI5 Reject dialog with the mandatory reason field visible.

Before capturing:

- use only mock data;
- hide browser bookmarks, unrelated tabs, usernames, URLs containing tenant details, and developer-tool panels;
- use a consistent browser width and zoom;
- ensure labels are readable and no popover covers important data;
- do not capture passwords, tokens, service keys, destination configuration, or Cloud Foundry details.

The root README already references these exact paths.
